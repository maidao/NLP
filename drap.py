

"""
test = ' hai@ba?bon.*%#89  '
s = data_raw.remove_ponctuation(test)
import logging
logging.basicConfig(format='%(asctime)s : %(levelname)s : %(message)s', level=logging.INFO)
pos = model.wv.most_similar(negative=w1)
print(model.wv.similarity(w1="vietnam",w2="french"))
print(model.wv.doesnt_match(["vietnam","vietnamese","food"]))

gram_n1 = data_raw.resto_ngram(string,1)
gram_n2 = data_raw.resto_ngram(string,2)
n_gram = gram_n1+gram_n2

nltk.download('stopwords')
"""
"""
    fig = plt.figure(figsize=(10, 10))
    ax = plt.axes(frameon=False)
    plt.setp(ax, xticks=(), yticks=())
    plt.subplots_adjust(left=0.0, bottom=0.0, right=1.0, top=0.9,
                        wspace=0.0, hspace=0.0)
    plt.scatter(X_embedded[:, 0], X_embedded[:, 1],
                c=data_resto['new_date'], marker="o")
    plt.show()

    """